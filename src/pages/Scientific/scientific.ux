<template>
  <div class="page">
    <div class="display-box">
      <text class="expr-text">{{displayExpression}}</text>
      <text class="result-text">{{result}}</text>
    </div>

    <div class="keyboard-list">
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('7')"><text class="num-text">7</text></div>
        <div class="key key-num" onclick="pressNumber('8')"><text class="num-text">8</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('9')"><text class="num-text">9</text></div>
        <div class="key key-soft" onclick="deleteLast"><text class="func-text">DEL</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('4')"><text class="num-text">4</text></div>
        <div class="key key-num" onclick="pressNumber('5')"><text class="num-text">5</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('6')"><text class="num-text">6</text></div>
        <div class="key key-soft" onclick="clearAll"><text class="func-text">AC</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('1')"><text class="num-text">1</text></div>
        <div class="key key-num" onclick="pressNumber('2')"><text class="num-text">2</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('3')"><text class="num-text">3</text></div>
        <div class="key key-equal" onclick="calculate"><text class="num-dark">=</text></div>
      </div>
      <div class="key-row">
        <div class="key key-num" onclick="pressNumber('0')"><text class="num-text">0</text></div>
        <div class="key key-soft" onclick="pressDot"><text class="num-text">.</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressParen('(')"><text class="num-text">(</text></div>
        <div class="key key-soft" onclick="pressParen(')')"><text class="num-text">)</text></div>
      </div>
      <div class="key-row">
        <div class="key key-op" onclick="pressOperator('+')"><text class="num-text">+</text></div>
        <div class="key key-op" onclick="pressOperator('-')"><text class="num-text">−</text></div>
      </div>
      <div class="key-row">
        <div class="key key-op" onclick="pressOperator('*')"><text class="num-text">×</text></div>
        <div class="key key-op" onclick="pressOperator('/')"><text class="num-text">÷</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressFunction('sin')"><text class="func-text">sin</text></div>
        <div class="key key-soft" onclick="pressFunction('cos')"><text class="func-text">cos</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressFunction('tan')"><text class="func-text">tan</text></div>
        <div class="key key-soft" onclick="pressFunction('log')"><text class="func-text">log</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressFunction('ln')"><text class="func-text">ln</text></div>
        <div class="key key-soft" onclick="pressSquare"><text class="func-text">x²</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressOperator('^')"><text class="func-text">xʸ</text></div>
        <div class="key key-soft" onclick="pressReciprocal"><text class="func-text">1/x</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressFactorial"><text class="func-text">n!</text></div>
        <div class="key key-soft" onclick="pressConstant('PI')"><text class="num-text">π</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressFunction('sqrt')"><text class="func-text">√x</text></div>
        <div class="key key-soft" onclick="clearAll"><text class="func-text">清空</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="deleteLast"><text class="func-text">删除</text></div>
        <div class="key key-soft" onclick="toggleSign"><text class="num-text">±</text></div>
      </div>
      <div class="key-row">
        <div class="key key-soft" onclick="pressExp"><text class="func-text">eˣ</text></div>
        <div class="key key-soft" onclick="pressConstant('e')"><text class="num-text">e</text></div>
      </div>
    </div>
  </div>
</template>

<style>
  .page {
    width: 192px;
    height: 490px;
    background-color: #000000;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .display-box {
    width: 172px;
    height: 100px;
    border-radius: 24px;
    background-color: #1a1a1a;
    margin-top: 10px;
    padding: 6px 10px;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: center;
  }

  .expr-text {
    width: 152px;
    height: 24px;
    color: #888888;
    font-size: 20px;
    text-align: right;
    white-space: nowrap;
    overflow-x: scroll;
  }

  .result-text {
    width: 152px;
    height: 40px;
    color: #ffffff;
    font-size: 34px;
    font-weight: 700;
    text-align: right;
    white-space: nowrap;
    overflow-x: scroll;
  }

  .keyboard-list {
    width: 192px;
    height: 380px;
    margin-top: 4px;
    overflow: scroll;
  }

  .key-row {
    width: 192px;
    height: 64px;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
  }

  .key {
    width: 88px;
    height: 60px;
    margin-left: 4px;
    margin-right: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .key-num {
    border-radius: 28px;
    background-color: rgba(128, 128, 128, 0.35);
    border-width: 1px;
    border-color: #888888;
  }

  .key-soft {
    border-radius: 28px;
    background-color: rgba(100, 149, 237, 0.35);
    border-width: 1px;
    border-color: #6495ed;
  }

  .key-op {
    border-radius: 28px;
    background-color: rgba(147, 112, 219, 0.35);
    border-width: 1px;
    border-color: #9370db;
  }

  .key-equal {
    border-radius: 28px;
    background-color: rgba(100, 149, 237, 0.35);
    border-width: 1px;
    border-color: #6495ed;
  }

  .num-text {
    color: #ffffff;
    font-size: 30px;
    font-weight: 700;
  }

  .num-dark {
    color: #ffffff;
    font-size: 30px;
    font-weight: 700;
  }

  .func-text {
    color: #ffffff;
    font-size: 20px;
    font-weight: 700;
  }
</style>

<script>
  import router from '@system.router'
  import storage from '@system.storage'
  import vibrator from '@system.vibrator'

  const HISTORY_KEY = 'calc_history_v2'

  export default {
    private: {
      expression: '',
      result: '0',
      justSolved: false,
      angleMode: 'DEG',
      vibrationOn: true
    },

    computed: {
      displayExpression() {
        return this.expression || '0'
      }
    },

    onInit() {
      var self = this
      storage.get({
        key: 'setting_vibration',
        success: function(data) {
          self.vibrationOn = data !== 'false'
        }
      })
    },

    doVibrate() {
      if (this.vibrationOn) {
        vibrator.vibrate({ mode: 'short' })
      }
    },

    onBackPress() {
      router.back()
      return true
    },

    clearAll() {
      this.doVibrate()
      this.expression = ''
      this.result = '0'
      this.justSolved = false
    },

    deleteLast() {
      this.doVibrate()
      if (this.justSolved) {
        this.clearAll()
        return
      }
      if (this.expression.length > 1) {
        this.expression = this.expression.substring(0, this.expression.length - 1)
        this.result = this.expression
      } else {
        this.expression = ''
        this.result = '0'
      }
    },

    pressNumber(num) {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = ''
        this.result = '0'
        this.justSolved = false
      }
      this.expression += num
      this.result = this.expression
    },

    pressDot() {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = '0.'
        this.justSolved = false
      } else {
        var part = this.getLastNumber(this.expression)
        if (part.indexOf('.') === -1) {
          this.expression += part === '' ? '0.' : '.'
        }
      }
      this.result = this.expression
    },

    pressOperator(op) {
      this.doVibrate()
      if (this.justSolved) {
        this.expression = this.result
        this.justSolved = false
      }
      if (this.expression === '') {
        if (op === '-') this.expression = '-'
        this.result = this.expression || '0'
        return
      }
      var last = this.expression.charAt(this.expression.length - 1)
      if (this.isOperator(last)) {
        this.expression = this.expression.substring(0, this.expression.length - 1) + op
      } else {
        this.expression += op
      }
      this.result = this.expression
    },

    pressParen(paren) {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = ''
        this.justSolved = false
      }
      this.expression += paren
      this.result = this.expression
    },

    pressFunction(name) {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = ''
        this.justSolved = false
      }
      this.expression += name + '('
      this.result = this.expression
    },

    pressConstant(name) {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = ''
        this.justSolved = false
      }
      this.expression += name === 'PI' ? 'π' : 'e'
      this.result = this.expression
    },

    pressExp() {
      this.doVibrate()
      if (this.justSolved || this.result === '错误') {
        this.expression = ''
        this.justSolved = false
      }
      this.expression += 'e^'
      this.result = this.expression
    },

    pressSquare() {
      this.doVibrate()
      this.expression = this.wrapLastValue(this.expression, 'square')
      this.result = this.expression
    },

    pressReciprocal() {
      this.doVibrate()
      this.expression = this.wrapLastValue(this.expression, 'reciprocal')
      this.result = this.expression
    },

    pressFactorial() {
      this.doVibrate()
      if (this.expression !== '') {
        this.expression += '!'
        this.result = this.expression
      }
    },

    toggleSign() {
      this.doVibrate()
      if (this.expression === '' || this.expression === '0') {
        this.expression = '-'
      } else if (this.expression.charAt(0) === '-') {
        this.expression = this.expression.substring(1)
      } else {
        this.expression = '-' + this.expression
      }
      this.result = this.expression || '0'
      this.justSolved = false
    },

    calculate() {
      this.doVibrate()
      try {
        if (this.expression === '') return
        var original = this.expression
        var expr = this.prepareExpression(this.expression)
        var factorial = function(n) {
          var num = Math.floor(n)
          if (num < 0 || num > 170) return NaN
          var res = 1
          for (var i = 2; i <= num; i++) res *= i
          return res
        }
        var log10 = function(n) { return Math.log(n) / Math.LN10 }
        var fn = new Function('factorial', 'log10', 'return ' + expr)
        var value = fn(factorial, log10)
        if (isNaN(value) || !isFinite(value)) {
          this.result = '错误'
        } else {
          this.result = this.formatResult(value)
          this.addHistory(original, this.result)
        }
        this.expression = ''
        this.justSolved = true
      } catch (e) {
        this.result = '错误'
        this.expression = ''
        this.justSolved = true
      }
    },

    addHistory(expr, result) {
      storage.get({
        key: HISTORY_KEY,
        success: function(data) {
          var list = []
          if (data) {
            try { list = JSON.parse(data) } catch (e) { list = [] }
          }
          list.push({ type: '计算器', expr: expr, result: result })
          if (list.length > 10) list = list.slice(1)
          storage.set({ key: HISTORY_KEY, value: JSON.stringify(list) })
        },
        fail: function() {
          storage.set({ key: HISTORY_KEY, value: JSON.stringify([{ type: '计算器', expr: expr, result: result }]) })
        }
      })
    },

    prepareExpression(expr) {
      var out = expr.replace(/×/g, '*').replace(/÷/g, '/').replace(/−/g, '-')
      out = out.replace(/π/g, 'Math.PI')
      out = out.replace(/\be\b/g, 'Math.E')
      out = out.replace(/\^/g, '**')
      out = out.replace(/square\(([^()]*)\)/g, 'Math.pow($1,2)')
      out = out.replace(/reciprocal\(([^()]*)\)/g, '(1/($1))')
      out = out.replace(/(\d+(\.\d+)?)!/g, 'factorial($1)')
      if (this.angleMode === 'DEG') {
        out = out.replace(/sin\(/g, 'Math.sin(Math.PI/180*')
        out = out.replace(/cos\(/g, 'Math.cos(Math.PI/180*')
        out = out.replace(/tan\(/g, 'Math.tan(Math.PI/180*')
      } else {
        out = out.replace(/sin\(/g, 'Math.sin(')
        out = out.replace(/cos\(/g, 'Math.cos(')
        out = out.replace(/tan\(/g, 'Math.tan(')
      }
      out = out.replace(/log\(/g, 'log10(')
      out = out.replace(/ln\(/g, 'Math.log(')
      out = out.replace(/sqrt\(/g, 'Math.sqrt(')
      return out
    },

    wrapLastValue(expr, fnName) {
      if (expr === '') return fnName + '(0)'
      var end = expr.length
      var start = end - 1
      while (start >= 0) {
        var ch = expr.charAt(start)
        if ((ch >= '0' && ch <= '9') || ch === '.' || ch === 'π' || ch === 'e') start--
        else break
      }
      var before = expr.substring(0, start + 1)
      var target = expr.substring(start + 1, end)
      if (target === '') return expr
      return before + fnName + '(' + target + ')'
    },

    formatResult(value) {
      var text = String(parseFloat(value.toFixed(10)))
      if (text.length > 12) text = value.toExponential(5)
      return text
    },

    getLastNumber(expr) {
      var number = ''
      for (var i = expr.length - 1; i >= 0; i--) {
        var ch = expr.charAt(i)
        if ((ch >= '0' && ch <= '9') || ch === '.') number = ch + number
        else break
      }
      return number
    },

    isOperator(ch) {
      return ch === '+' || ch === '-' || ch === '*' || ch === '/' || ch === '^'
    }
  }
</script>
